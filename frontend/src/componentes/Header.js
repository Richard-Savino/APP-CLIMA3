import React from 'react';

const Header = () => {
    return (
        <header style={headerStyle}>
            <h1>CLIMA: LN+</h1>
        </header>
    );
};

const headerStyle = {
    background: '',
    color: '#fff',
    padding: '10px 0',
    textAlign: 'center'
};

export default Header;
